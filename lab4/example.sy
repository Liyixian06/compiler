int whileIf() {
  // 鑰冨療璧�?�?艰��鍙�?
  int a;
  a = 0;
  int b;
  b = 0;
  // 鑰冨療while/if璇�鍙ュ拰鍏崇郴杩愮�?
  while (a < 100) { 
    if (a == 5) {
      b = 25;
    }
    else if (a == 10) {
      b = 42;  
    }
    else {
      b = a * 2;
    }
    a = a + 1;
  }
  // 鑰冨療return璇�鍙�
  return (b);
}

int main(){
  int a = getint(); // 鑰冨療SysY杩愯�屾椂搴�?
  int b = 30;
  int c= a - -5 + b + -5; // 鑰冨療琛ㄨ揪�?�?
  return (whileIf()); // 鑰冨療鍑芥暟璋冪�?
}
